var mpFunc = function(){
	emit(this.class,this.marks);
};

var redFun = function(key,vals){
	return Array.sum(vals);
};

db.Student.mapReduce(
	mpFunc,
	redFunc,
	{out:"total_marks"}
);

db.total_marks.find();


